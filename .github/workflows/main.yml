name: Trimite Stirile Zilnice

on:
  schedule:
    - cron: '0 5 * * *' # Rulează zilnic la ora 08:00 a României
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Descarcă codul din repository
        uses: actions/checkout@v4

      - name: Configurează Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Instalează librăriile necesare
        run: pip install -r requirements.txt

      - name: Rulează scriptul de știri
        env:
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
        run: python news_bot.py

      - name: Salvează și publică site-ul web
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "actions@github.com"
          git add index.html
          git diff-index --quiet HEAD || git commit -m "Actualizat site stiri [skip ci]"
          git push
